import { CreateTicketForm } from "@/components/tickets/CreateTicketForm";

/**
 * DEC-009a / DEC-009b: `/tickets/new` create ticket page (UIF-002).
 */
export default function NewTicketPage() {
  return <CreateTicketForm />;
}
