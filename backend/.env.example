# Example environment file for local PostgreSQL.
# Copy to backend/.env (or export in your shell). Do NOT commit real secrets.
#
#   cp backend/.env.example backend/.env
#
# backend/.env is gitignored.

DB_URL=jdbc:postgresql://localhost:5432/ticket_management
DB_USERNAME=ticket
DB_PASSWORD=change-me-locally
